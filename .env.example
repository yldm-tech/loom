# TypeSafe API key (https://typesafe.ai) — powers every Jev judgement.
JEV_TOKEN=

# Any OpenAI-compatible chat completions endpoint for copywriting.
# Defaults below point at EveryAPI; swap for OpenAI, OpenRouter, a local
# llama.cpp server, or anything else that speaks the same protocol.
LLM_TOKEN=
LLM_BASE_URL=https://api.everyapi.ai/v1
LLM_MODEL=claude-haiku-4-5
